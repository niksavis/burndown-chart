# -*- mode: python ; coding: utf-8 -*-
"""
PyInstaller spec file for Burndown Chart main application.
Defines how to bundle the application and all its dependencies.
"""

import os
from pathlib import Path

# Get project root - when running with pyinstaller from project root,
# spec file paths are relative to spec file location (build/)
# So we need to go up one level
SPEC_DIR = Path(os.path.abspath(SPECPATH))
PROJECT_ROOT = SPEC_DIR.parent

block_cipher = None

# Data files to bundle (source_path, dest_path_in_bundle)
# These paths are relative to the project root
datas = [
    # Frontend assets: CSS, JS, images, manifest
    (str(PROJECT_ROOT / 'assets'), 'assets'),
    # Report assets: HTML templates, CSS, JS for report generation
    (str(PROJECT_ROOT / 'report_assets'), 'report_assets'),
    # Third-party licenses (generated by collect_licenses.ps1)
    (str(PROJECT_ROOT / 'licenses'), 'licenses'),
    # Project LICENSE file (extracted on first run)
    (str(PROJECT_ROOT / 'LICENSE'), '.'),
    # Changelog for About dialog
    (str(PROJECT_ROOT / 'changelog.md'), '.'),
]

# Hidden imports that PyInstaller might miss
hiddenimports = [
    'dash',
    'dash.dependencies',
    'dash_bootstrap_components',
    'plotly',
    'pandas',
    'numpy',
    'scipy',
    'scipy.stats',
    'requests',
    'waitress',
    'pyperclip',
    'dotenv',
    'networkx',
    'pydantic',
    'sqlite3',
    'logging.handlers',
    'typing_extensions',
]

# Packages to exclude from the bundle
excludes = [
    # Test frameworks and tools
    'pytest',
    'pytest_cov',
    'pytest_mock',
    'pytest_asyncio',
    'playwright',
    'selenium',
    # Development tools
    'pip_tools',
    'pip_licenses',
    'pyinstaller',
    'setuptools',
    'wheel',
    'pip',
    # Build tools
    'distutils',
    'setuptools',
    # GUI frameworks not used
    'tkinter',
    'wx',
    'PyQt5',
    'PyQt6',
    'PySide2',
    'PySide6',
    # Test modules
    'test',
    'tests',
    'unittest',
    '_pytest',
    'py.test',
    # Documentation tools
    'sphinx',
    'docutils',
    # Type checking
    'mypy',
]

a = Analysis(
    [str(PROJECT_ROOT / 'app.py')],  # Use absolute path
    pathex=[],
    binaries=[],
    datas=datas,
    hiddenimports=hiddenimports,
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=excludes,
    win_no_prefer_redirects=False,
    win_private_assemblies=False,
    cipher=block_cipher,
    noarchive=False,
)

pyz = PYZ(a.pure, a.zipped_data, cipher=block_cipher)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.zipfiles,
    a.datas,
    [],
    name='BurndownChart',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=False,  # Disabled to avoid false positives with antivirus
    upx_exclude=[],
    runtime_tmpdir=None,
    console=False,  # No terminal window - runs as background service with browser UI
    disable_windowed_traceback=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
    icon=str(PROJECT_ROOT / 'assets' / 'icon.ico') if (PROJECT_ROOT / 'assets' / 'icon.ico').exists() else None,
    version=str(SPEC_DIR / 'version_info.txt') if (SPEC_DIR / 'version_info.txt').exists() else None,
)
