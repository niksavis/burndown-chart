
# Normalize all text files to LF in the repository
* text=auto eol=lf

# Use bd merge for beads JSONL files
.beads/issues.jsonl merge=beads

# Normalize line endings for .beads files
.beads/** text=auto
